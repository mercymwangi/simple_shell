#include <stdio.h>
#include "shell.h"

/**
 * main - entry point
 *
 * Return: 0 Always (success)
 */
int main(void)
{
	int a = 10;
	int b = 15;

	int sum = (a + b);

	printf("Result: %d\n", sum);

	return (0);
}
